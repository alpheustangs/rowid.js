{
    "private": true,
    "type": "module",
    "scripts": {
        "test": "cross-env ROWID_CLI_DEBUG=1 vitest run",
        "": ""
    },
    "dependencies": {
        "execa": "^9.3.0",
        "rowid-cli": "workspace:^"
    },
    "devDependencies": {
        "cross-env": "^7.0.3",
        "vite": "^5.4.0",
        "vitest": "^2.0.5"
    }
}
